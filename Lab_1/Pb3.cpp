//	Insertion dans un tableau : Écrire un programme qui demande à l’utilisateur un entier et l’insère au bon
//	endroit dans un tableau trié pour que le tableau reste trié.Écrire une fonction pour la lecture de l’entier,
//	qui redemande si l’entrée est invalide, par exemple si l’usager entre des lettres(vous pouvez lire comme
//	un texte et utiliser la fonction stoi, voir la documentation sur cppreference.com; si vous voulez traiter les
//	exceptions, regardez la syntaxe dans « Exceptions » de « De Python à C++ » sur le site Moodle).Utiliser
//	un tableau C ou un std::array, de taille suffisante pour contenir toutes les valeurs après l’insertion(pas un
//		std::vector, qui a une méthode pour insérer).
//	Exemple :
//	entier = 5 tableau avant insertion = { 1, 3, 4, 7, 9 } tableau après insertion = { 1, 3, 4, 5, 7, 9 }
//	Pour le tableau trié vous pouvez déclarer un tableau contenant des valeurs déjà triées et utiliser une
//	variable qui indique le nombre d’éléments dans le tableau.

#include <iostream>
using namespace std;

void getInt()
{

	while (true)\
	{
		
	}
}

int main()
{
	getInt();
		//	 try {
		//	quelque_chose();
		//}
		//catch (const type_exception& variable_exception) { // Toujours avec &
		//	faire_en_cas_exception(variable_exception);
		//}
}